import { StyleSheet } from 'react-native';

const styled = StyleSheet.create({
    conyainerButton:{
        paddingTop: 50
    },
    textButtonClean:{
        color: '#fff'
    }
});

export default styled;